import { atom } from 'recoil';

export const openAddAdminPopUp = atom({
    key: 'openAddAdmin',
    default: false
});

export const openAddAppLink = atom({
    key: 'openAddApp',
    default: false
})
